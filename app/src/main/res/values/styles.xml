<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>
    <!-- 自定义ActionBar样式，重设ActionBar背景，隐藏actionbar左侧的应用图标和标题 -->
    <style name="myactionbar_style" parent="@android:style/Widget.Holo.Light.ActionBar">
        <!-- 在样式里面直接替换掉actionbar的背景，避免在有的窗口进入慢时导致进入时显示系统默认的actionbar -->
        <item name="android:background">#0066ff</item>
        <!-- 在样式里面去掉acionbar的应用图标和标题 -->
        <!--<item name="android:displayOptions">none</item>-->
    </style>
    <style name="MyActivityTheme">
        <item name="android:actionBarStyle">@style/myactionbar_style</item>
    </style>
</resources>
